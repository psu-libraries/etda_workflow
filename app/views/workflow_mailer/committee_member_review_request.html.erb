<%= simple_format(t( "#{current_partner.id}.partner.email.committee_member_review_requested.message",
       full_name: @author.full_name,
       title: @submission.title,
       mail_link: mail_to(t("#{current_partner.id}.partner.email.address")),
       degree_type: @submission.degree_type,
       review_url: link_to(@review_url, @review_url),
       committee_member_name: @committee_member.name,
       core_member_note: @core_member_note).html_safe) %>
<%= simple_format(t("#{current_partner.id}.partner.email.contact_information").html_safe) %>
