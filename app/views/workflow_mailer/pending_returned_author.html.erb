Dear <%= @author.full_name %>,

<%= simple_format(t( "#{current_partner.id}.partner.email.pending_returned_author.message",
        link: link_to(t( "#{current_partner.id}.partner.email.pending_returned_author.link_text"), @etd_site),
        degree_type: @submission.degree_type, etd_site: @etd_site ).html_safe) %>
<%= simple_format(t("#{current_partner.id}.partner.email.contact_information").html_safe) %>
