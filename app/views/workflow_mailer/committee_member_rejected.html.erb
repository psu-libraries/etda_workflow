<%= t( "#{current_partner.id}.partner.email.committee_member_rejected.message",
       degree_type: @submission.degree_type,
       partner_name: current_partner.name,
       author_name: @author.full_name,
       committee_member_name: @committee_member.name,
       url: @mailer_base_url ) %>
