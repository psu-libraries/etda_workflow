Hello,

<%= simple_format t( "#{current_partner.id}.partner.email.pending_returned_committee.message",
       author_name: @author.full_name,
       title: @submission.title,
       ).html_safe %>
<%= simple_format(t("#{current_partner.id}.partner.email.contact_information").html_safe) %>
