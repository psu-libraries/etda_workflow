Dear <%= @submission.author.full_name %>,

<%= simple_format t( "#{current_partner.id}.partner.email.committee_rejected_author.message",
       link: link_to(t( "#{current_partner.id}.partner.email.committee_rejected_author.link_text"), @etd_site),
       degree_type: @submission.degree_type,
       partner_name: current_partner.name,
       author_name: @author.full_name,
       review_results: @review_results,
       etd_site: @etd_site).html_safe %>
<%= simple_format(t("#{current_partner.id}.partner.email.contact_information").html_safe) %>
