Hello University Libraries Cataloging,

The following Electronic Theses/Dissertations were released as Open Access between <%= @date_range.to_s %>:

<% @submissions.each do |submission| %>
<%= t( "#{current_partner.id}.partner.email.open_access_release.message",
       full_name: "#{submission.author.first_name} #{submission.author.last_name}",
       title: submission.title,
       degree_type: submission.degree.degree_type.name,
       graduation_year: "#{submission.semester} #{submission.year}",
       released_on: submission.released_for_publication_at.strftime('%D'))%>

<% end %>
Thank you,
The Graduate School at Penn State
