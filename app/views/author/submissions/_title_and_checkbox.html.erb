      <%= f.input :title, required: true, label: t("#{current_partner.id}.partner.document_title_label"), hint: 'Please do not use words that are entirely uppercase, unless necessary (i.e. acronyms, chemical equations, etc.)', placeholder: 'Ex: This is a Title Without Uppercase Words' %>
      <%= f.input :allow_all_caps_in_title, label: 'Allow completely uppercase words in title', wrapper: :checkbox %>
